import { Box } from "@mui/material"

const Margin = () => {
  return (
    <Box
      sx={{
        height: ".75rem",
        // backgroundColor: (theme) =>
        //   theme.palette.mode === "light"
        //     ? "rgba(255, 0, 0, 0.1)"
        //     : "rgb(255 132 132 / 25%)",
      }}
    />
  )
}

export default Margin
