import withPrivateRoute from "hoc/withPrivateRoute"
import withStandardLayout from "hoc/withStandardLayout"
import React from "react"

const ReleaseStats = () => {
  return <div></div>
}

export default withPrivateRoute(withStandardLayout(ReleaseStats))
